#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    vector<vector<string>> partition(string s) {
        vector<vector<string> > ret;
    }
private:
    void partition(string s, int from, int to, vector<string> &part) {

    }
};